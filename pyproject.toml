[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "radjax-student"
version = "0.1.0"
description = "Student-side RADJAX training and evaluation scaffold"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26",
  "PyYAML>=6.0",
  "radjax-contract @ git+https://github.com/nova-rey/RADJAX-Contract.git@a33a0a0d90a57e5cd67155c01f054d3e7a04dc0d",
]

[project.scripts]
radjax-student = "radjax_student.cli.main:main"

[project.optional-dependencies]
jax = [
  "jax[cpu]==0.4.38",
]
dev = [
  "pytest>=8.0",
  "ruff>=0.6",
]
test-jax = [
  "pytest>=8.0",
  "ruff>=0.6",
  "jax[cpu]==0.4.38",
]

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
markers = ["jax: requires the optional JAX CPU test extra"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
