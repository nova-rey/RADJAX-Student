# Phase 6 Execution Ledger

This append-only ledger records completed Phase 6 checkpoints and their
bounded evidence. It does not expand Phase 5 claims or authorize future work.

## P6.1 — Baseline Scale Characterization and JAX Ownership

- Authority preflight matched Student `b24565d`, Contract `a33a0a0d`, and Tome
  `b78821c` exactly. The ordinary Tome v6 smoke artifact has four examples and
  19 valid tokens; P6.U1 is therefore mandatory before P6.6.
- A fresh installed environment distinguished 31 polluted-install fixture and
  subprocess failures from the three direct behavior JAX-import blockers and
  their dependent receipt failures.
- `BehaviorJaxPassAdapterV1` now owns only lazy behavior-pass input conversion,
  differentiation, gradient norm, and tree inspection. Corridor, exemplar,
  and held-out policy modules no longer import JAX directly. The post-repair
  dependency audit covers 202 modules and passes.
- The Phase 5 focused regression suite passes 78 tests. The raw CPU baseline,
  including direct configurable forward probes, 30 synchronized repetitions,
  B=2 rejection, and a fresh-subprocess ordinary-fixture materialization
  probe, is recorded in [P6_1_RAW_BASELINE.json](P6_1_RAW_BASELINE.json) and
  summarized by [P6_1_BASELINE.json](P6_1_BASELINE.json).
- A second clean installed environment completed the full suite: 1,103 passed
  (with 103 existing deprecation warnings).
- No generic behavioral lifecycle, production materializer, B>1 support,
  structural RWKV generalization, or accelerator qualification is claimed.
