# Student Backends

The initial scaffold provides `tiny_debug`, a NumPy-only backend for import,
registry, and one-step training tests.

Production QRWKV/RWKV backends will be migrated after Contract boundaries are
stable.

